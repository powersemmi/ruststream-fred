# cargo-generate manifest for the Redis Streams RustStream starter.
#
# Render it with:
#   cargo generate --git https://github.com/powersemmi/ruststream-fred templates/redis-stream --name my-service
#
# `{{project-name}}` (the --name value) is a cargo-generate built-in; no placeholders need declaring.
[template]
description = "A Redis Streams (consumer-group) RustStream starter service"
